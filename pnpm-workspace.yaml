packages:
  - "."

allowBuilds:
  esbuild: true
  workerd: true
  sharp: true
  lefthook: true
  # msw's postinstall only copies the browser Service Worker script; these tests
  # run msw/node, so the script is not needed.
  msw: false

minimumReleaseAgeExclude:
  - "@cloudflare/workers-types@5.20260711.1"
  - "undici@7.29.0"

overrides:
  # GHSA-67mh-4wv8-2f99: drizzle-kit -> @esbuild-kit/esm-loader (deprecated) -> @esbuild-kit/core-utils -> esbuild@0.18.20 (vulnerable)
  "@esbuild-kit/core-utils>esbuild": "^0.28.1"
  # GHSA-8xcm-r25x-g524 / GHSA-4cwx-7wf7-3272 / GHSA-m8rv-5g2x-5cg5 / GHSA-jr45-8vmc-qm54 / GHSA-v3r7-h72x-cjcm: wrangler/vitest -> miniflare/jsdom -> undici@7.28.0 (vulnerable)
  "undici": "^7.29.0"
  vite: "catalog:"
  vitest: "catalog:"
catalog:
  vite: npm:@voidzero-dev/vite-plus-core@0.2.4
  vitest: 4.1.11
  vite-plus: 0.2.4
peerDependencyRules:
  allowAny:
    - vite
    - vitest
  allowedVersions:
    vite: "*"
    vitest: "*"
